#include <bits/stdc++.h>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int n, m, x, y, danger = 1;
    cin >> n >> m;
    set<int> pote;

    for(int i = 0; i < m; i++) {
        cin >> x >> y;

    }

    danger = 

    cout << danger;


    return 0;
}